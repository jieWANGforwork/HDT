# External Data

Use this directory for downloaded or mounted artifacts before moving them into
the expected `data/processed/<dataset>/` layout.

Prepared dataset folders:

- `reddit/`
- `xing/`
- `lfm/`
- `lfmalbum/`
