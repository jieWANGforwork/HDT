# Raw Data

Place optional raw source files here before preprocessing.

Prepared dataset folders:

- `reddit/`
- `xing/`
- `lfm/`
- `lfmalbum/`

Raw data is ignored by git except for README and `.gitkeep` placeholders.
