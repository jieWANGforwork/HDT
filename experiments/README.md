# Experiments

Use `configs/experiments/` for reproducible experiment settings. This directory
is reserved for notes, result summaries, and non-large metadata.

Prepared folders:

- `reddit/`
- `xing/`
- `lfm/`
- `lfmalbum/`
- `ablation/`
